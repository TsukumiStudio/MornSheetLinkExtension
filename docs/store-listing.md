# Chrome ウェブストア掲載内容

## 名前

Morn Sheet Link Extension

## 概要

Google スプレッドシートの選択範囲を、ファイル名_タブ名_範囲の表示名付きハイパーリンクとしてコピーします。

## 詳細説明

スプレッドシートの範囲を共有するとき、長いURLの代わりに内容が分かるハイパーリンクとしてコピーできます。

例：売上管理_9月_B2:D10

使い方
1. Google スプレッドシートでセルや行・列の範囲を選択します。
2. 右クリックし、「セルでの他の操作項目を表示」を開きます。行や列を選択している場合は対応するメニューを開きます。
3. 「この範囲へのリンクを作成 Ex」を選びます。
4. リンクの貼り付けに対応したアプリへ貼り付けます。

HTMLの貼り付けに対応したアプリでは、選択範囲へのクリック可能なリンクとして貼り付けられます。プレーンテキストのみのアプリでは表示名だけになります。貼り付け先によってリンクの扱いは異なります。

通常のセル、連続範囲、行全体、列全体に対応。日本語・英語のスプレッドシートメニューに対応します。名前付き範囲や離れた複数範囲には対応していません。

データの外部送信、広告、アクセス解析はありません。
Google の公式製品ではありません。Google スプレッドシートの画面変更によって機能の調整が必要になることがあります。

## プライバシー申告用

- 単一用途：Google スプレッドシートの選択範囲へのリンクを、ファイル名・タブ名・範囲を含む表示名でコピーする。
- clipboardWrite の理由：ユーザーによるEx項目の実行時に、HTMLリンクとプレーンテキストをクリップボードへ書き込むため。
- サイトアクセスの理由：スプレッドシートのメニューへの項目追加と、開いているファイル名・タブ名・選択範囲・URLの取得に必要。
- リモートコード：使用しない。実行するコードはすべて拡張のZIPに含む。
- データ処理：ページ情報をブラウザ内で処理し、クリップボードに書き込む。開発者への収集・送信・永続保存は行わない。

## 公開手順

1. `node --test content.test.cjs` を実行する。
2. `python3 scripts/package.py` でアップロード用ZIPを作る。
3. 開発者ダッシュボードへZIPをアップロードする。
4. この説明文、アイコン、スクリーンショット、小さいプロモーション画像を登録する。
5. 公開したプライバシーポリシーのURLと、実際のサポート連絡先を設定する。
6. ダッシュボードの申告を確認して審査に提出する。

アカウント登録・審査提出・ストア公開は未実施です。

仕様確認元：[公開準備](https://developer.chrome.com/docs/webstore/prepare)、[画像要件](https://developer.chrome.com/docs/webstore/images)、[公開手順](https://developer.chrome.com/docs/webstore/publish/)。

## 申請画面への入力

- 言語：日本語
- カテゴリ：Productivity（生産性）のうち、画面に表示される Tools（ツール）相当を選択。
- 価格：無料。課金・外部購入はありません。
- リモートコードを使用しますか：いいえ。
- データ収集：開発者や第三者への収集・送信はありません。取得するページ情報は上記「データ処理」のとおり端末内で処理します。データの販売、用途外利用、信用評価への利用は行いません。
- ウェブサイト／サポート／プライバシーポリシーURL：公開先を確定し、アクセスできる実際のURLを登録してください。未公開URLを入力しないでください。

## アップロードするファイル

| 項目 | ファイル |
| --- | --- |
| 拡張機能のZIP | `dist/MornSheetLinkExtension-0.1.2.zip` |
| ストアアイコン | `icons/icon-128.png` |
| スクリーンショット | `assets/screenshot-1280x800.png` |
| 小さいプロモーション画像 | `assets/promo-440x280.png` |

`assets/ogp-editable.png` はSNS向けFHD画像です。ストアのスクリーンショット欄は1280×800または640×400のため、FHD画像をそのまま登録しません。PSDは申請ZIPに含めません。

## 審査担当者への操作説明

拡張機能専用のアカウントや購入は不要です。Google スプレッドシートを利用できるGoogleアカウントと、編集可能なテスト用シートを使用してください。

1. 拡張機能をインストール後、Google スプレッドシートを再読み込みします。
2. ファイル名を「売上管理」、タブ名を「9月」にします。セルへのデータ入力は不要です。
3. 名前ボックスで `B2:D10` を選択します。
4. 右クリック →「セルでの他の操作項目を表示」→「この範囲へのリンクを作成 Ex」を実行します。
5. 「コピーしました」の表示を確認します。
6. HTMLの貼り付けに対応した文書エディタへ通常の貼り付けを行います。「売上管理_9月_B2:D10」がリンクになり、リンク先に元のシートの `gid` と `range=B2%3AD10` が含まれることを確認します。
7. 別タブでも同じ操作を行い、タブ名とリンク先が更新されることを確認します。

選択範囲のセル内容は読み取りません。リンクを共有しても、スプレッドシート自体の共有権限は変更しません。プレーンテキストとして貼り付けた場合は表示名のみになります。

## 提出前に残る確認

- 公開済みのプライバシーポリシーURLとサポート窓口の登録。
- Chrome上でコピー後、実際の貼り付け先でリンク名・移動先を確認（メニュー表示とコピー成功までは確認済み）。
- 開発者アカウントの登録状況と連絡先認証の確認。
- 審査提出後の公開方法は、初回は承認後に手動公開を選ぶ。

準備した素材だけでは審査提出は完了しません。ダッシュボードでのアップロード・申告・提出は未実施です。
